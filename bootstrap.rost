// This program is supposed to be the initial bootstrapping of the language.

fn main(argc: int, argv: &&char) {
	let first = argv + 1;

	printf("0x%x\n", argc);
	printf("0x%x\n", argv);
	printf("%s\n", *first);
}